package parser

// HeroName returns the hero's name given the image in base64 format.
func HeroName(imgAsBase64 string) string {
	// client := gosseract.NewClient()
	// defer client.Close()

	// img := image.FromBase64(imgAsBase64)

	// heroName, _ := client.Text()
	// return heroName
	return ""
}
